{
    "$schema": "https://turbo.build/schema.json",
    "globalDependencies": ["**/.env.*local"],
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**", ".build/**", ".svelte/**"]
        },
        "check": {},
        "lint": {},
        "dev": {
            "cache": false,
            "persistent": true
        }
    }
}